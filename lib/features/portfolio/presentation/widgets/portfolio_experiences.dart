import 'package:flutter/material.dart';

class PortfolioExperiences extends StatelessWidget {
  const PortfolioExperiences({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 180,
      color: const Color(0xFF202024),
      child: const Center(
        child: Text('Experiences'),
      ),
    );
  }
}
